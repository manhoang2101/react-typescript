import { createStyles } from "@material-ui/core";

const style = () =>
  createStyles({
    table: {
      maxWidth: "100%",
    },
    card: {
      width: "960px",
      margin: "0 auto",
    },
  });
export default style;
